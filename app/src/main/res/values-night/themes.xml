<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for Dark mode. -->
    <style name="Base.Theme.NewApp" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/dark_purple</item>
        <item name="colorPrimaryVariant">@color/dark_purple_variant</item>
        <item name="android:statusBarColor">@color/dark_purple</item>
        <item name="android:navigationBarColor">@color/dark_purple</item>

        <!-- Default Text Colors -->
        <item name="android:textColorPrimary">@color/text_color</item>
        <item name="android:textColorSecondary">@color/text_color</item>


    </style>

    <style name="Theme.NewApp" parent="Base.Theme.NewApp" />
</resources>
